services:
  - type: web
    name: refsys-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m refsys server --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.2
